########################################
# 生产环境变量示例（请复制为 .env.prod 并修改为强口令/真实域名）
# 安全提示：
# - 生产必须使用强随机密码与专用访问密钥
# - 建议数据库与对象存储仅在内网访问，由外部反代接入应用层服务
########################################

# 应用：前端访问后端 API 的基础地址（生产域名 + 前缀按需）
NEXT_PUBLIC_API_BASE_URL=https://your-domain.example.com/api

# 数据库：PostgreSQL 连接信息（容器内部网络）
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=yolonereus
POSTGRES_USER=yn_app
POSTGRES_PASSWORD=ChangeMe_Strong!   # 必须替换为强口令

# Redis：缓存与队列
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=                   # 如开启 requirepass，请在此设置强口令

# MinIO：对象存储（S3 兼容）— 强烈建议自定义强凭证
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=ChangeMe_Strong!  # 必须替换为强口令
S3_ENDPOINT=http://minio:9000         # 应用容器内部访问地址；若外部访问请替换为反代域名
S3_ACCESS_KEY=minioadmin              # 应用访问用 AK（生产建议单独用户）
S3_SECRET_KEY=ChangeMe_Strong!        # 应用访问用 SK（务必强口令）
S3_BUCKET=yolo-samples                # 生产桶名（建议与环境区分，如 yolo-samples-prod）
