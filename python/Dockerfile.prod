# Prod Dockerfile for ML Python (placeholder)
FROM python:3.12-slim
WORKDIR /app
ENV PIP_DISABLE_PIP_VERSION_CHECK=1 \
    PIP_NO_CACHE_DIR=1
COPY requirements.txt ./
RUN pip install -r requirements.txt
# replace with `uvicorn app:app --host 0.0.0.0 --port 8001` when code is ready
CMD ["sh", "-c", "echo 'ml-python prod container ready'; tail -f /dev/null"]
