########################################
# 开发环境变量示例（可复制为 .env.dev 使用）
# 说明：
# - 与 docker-compose.dev.yml 保持一致
# - 前端使用 NEXT_PUBLIC_* 变量会被打包注入浏览器
########################################

# 应用：前端访问后端 API 的基础地址（开发本机）
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# 数据库：PostgreSQL 连接信息（容器内部网络）
POSTGRES_HOST=postgres        # docker compose 中服务名
POSTGRES_PORT=5432
POSTGRES_DB=yolonereus        # 默认数据库名
POSTGRES_USER=yn_app          # 应用账号
POSTGRES_PASSWORD=yn_app_pass # 开发弱口令，勿用于生产

# Redis：缓存与队列（容器内部网络）
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=               # 开发默认空密码

# MinIO：对象存储（S3 兼容）
MINIO_ROOT_USER=minioadmin        # MinIO 管理员账号（开发默认）
MINIO_ROOT_PASSWORD=minioadmin    # MinIO 管理员密码（开发默认）
S3_ENDPOINT=http://minio:9000     # 应用侧访问 MinIO 的内部地址
S3_ACCESS_KEY=minioadmin          # 应用访问用 AK（可与 ROOT 相同）
S3_SECRET_KEY=minioadmin          # 应用访问用 SK（可与 ROOT 相同）
S3_BUCKET=yolo-samples            # 默认桶名（首次需手动创建或后续脚本创建）
